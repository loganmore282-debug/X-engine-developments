// Furagemz app shell — Phase 1 placeholder.
// Full screens (auth, home, gem tiers, team, account, history) land in Phase 3
// as this file grows; the build pipeline below stays the same.
document.getElementById('enterBtn').addEventListener('click', () => {
  alert('Furagemz is being built — check back soon.');
});

